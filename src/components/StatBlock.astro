---
const { value, label } = Astro.props;
---

<div class="rounded-2xl border border-white/10 bg-white/10 px-6 py-8 text-center">
  <p class="text-3xl font-semibold text-accent">{value}</p>
  <p class="mt-2 text-sm text-slate-200">{label}</p>
</div>
